import React from 'react';

export default class profile extends React.Component{
    constructor(props)
    super(props)
}
    render(){
        let profilCount = this.state.profileData
    render(
        <div>
            I am in profile page
        <div> Total count is : {profileCount}
            </div>
        </div>
    )
    }
}